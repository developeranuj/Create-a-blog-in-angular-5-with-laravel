# Create-a-blog-in-angular-5-with-laravel

Installation
composer install
npm install
Create .env file (can be based on .env.example)

php artisan key:generate

Run Laravel

php artisan serve

ng serve

